/* eslint-disable no-unused-vars */
/* eslint-disable no-shadow */
enum EnumResourceType {
  /** Resource by DEFAULT uses a entity */
  DEFAULT = 'default',
  /** Resource LIST uses an entity array */
  LIST = 'list',
  /** Resource SELECT used for selects in frontend app */
  SELECT = 'select',
}

export default EnumResourceType;
